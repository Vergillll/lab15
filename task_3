N = int(input())
A = []
k = 0
for i in range(0, N):
    A.append(float(input()))

print(A)

for i in range(0, N):
    if A[i] % 2 != 0:
        k = A[i]

for i in range(0, N):
    if A[i] % 2 != 0:
        A[i] += k

print(k)
print(A)
